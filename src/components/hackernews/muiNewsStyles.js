import {
    Button,
    Grid,
    styled,
} from "@mui/material";

export const StyledGrid = styled(Grid)(() => ({
    display: "flex",
    marginBottom: "2%",
  }));
export const StyledButton = styled(Button)(() => ({
    margin: "3%",
  }));
  